/// example: 012-match-result
/// level: 1
/// concept: match on Result
/// expected_diagnostics: none
/// test_status: draft
/// ai_rule: Every match on Result must handle both Ok and Err.

match parseAge("42") {

  Ok(age) =>
    print(age)

  Err(err) =>
    print(err)
}
